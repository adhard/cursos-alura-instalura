import { StyleSheet } from 'react-native'

const estilo = StyleSheet.create({
    inline: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center'
    },
    imgSend: {
        width: 20,
        height: 20,
        marginLeft: 2,
        marginRight: 5
    }
})

export default estilo